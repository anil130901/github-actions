name: Debug Event Context
on: [push, pull_request]

jobs:
  inspect-payload:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub Event Object
        env:
          EVENT_JSON: ${{ toJSON(github) }}
        run: |
          echo "$EVENT_JSON"
          echo "branch name: ${{ github.ref }}"
